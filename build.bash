docker build -t ros_noetic .

# --env="LIBGL_ALWAYS_INDIRECT=1" 

# source devel/setup.bash &&
# export ROS_MASTER_URI=http://192.168.0.132:11311 &&
# export ROS_IP=$(hostname -I | awk '{print $1;}') &&
# export ROS_HOSTNAME=$ROS_IP 